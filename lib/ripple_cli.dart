/// Ripple CLI — discover packages and run commands or scripts via ripple.yaml.
library;

// Public API surface will grow as config, discovery, and commands land.
// Internal modules live under `src/` and are not part of the stable export set yet.
